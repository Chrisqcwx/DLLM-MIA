Mtc5depend-4-0.1-layer-0-1-least1-mask-0.10-0.10:
  module: "mtc5depend"
  lambda_penalty: 0.1
  leastm: 1
  weight_start_layer_ratio: 0
  weight_end_layer_ratio: 1
  steps: 4
  subset_size: 8          # l for each local subset vote
  num_subsets: 128        # number of subsets per step
  l_schedule: "linear"    # or "geometric"
  min_mask_frac: 0.10     # ~5% of valid tokens masked at early steps
  max_mask_frac: 0.10     # up to ~50% by the last step
  batch_size: 8
  max_length: 512
  reference_model_path: "GSAI-ML/LLaDA-8B-Base"
  reference_device: "cuda"
  seed: 42
  save_metadata: false
  metadata_dir: ""  # Set via SAMA_METADATA_DIR env variable, or specify a path here

Mtc5depend-4-0.1-layer-0-1-least1-mask-0.20-0.20:
  module: "mtc5depend"
  lambda_penalty: 0.1
  leastm: 1
  weight_start_layer_ratio: 0
  weight_end_layer_ratio: 1
  steps: 4
  subset_size: 8          # l for each local subset vote
  num_subsets: 128        # number of subsets per step
  l_schedule: "linear"    # or "geometric"
  min_mask_frac: 0.20     # ~5% of valid tokens masked at early steps
  max_mask_frac: 0.20     # up to ~50% by the last step
  batch_size: 8
  max_length: 512
  reference_model_path: "GSAI-ML/LLaDA-8B-Base"
  reference_device: "cuda"
  seed: 42
  save_metadata: false
  metadata_dir: ""  # Set via SAMA_METADATA_DIR env variable, or specify a path here

Mtc5depend-4-0.1-layer-0-1-least1-mask-0.05-0.05:
  module: "mtc5depend"
  lambda_penalty: 0.1
  leastm: 1
  weight_start_layer_ratio: 0
  weight_end_layer_ratio: 1
  steps: 4
  subset_size: 8          # l for each local subset vote
  num_subsets: 128        # number of subsets per step
  l_schedule: "linear"    # or "geometric"
  min_mask_frac: 0.05     # ~5% of valid tokens masked at early steps
  max_mask_frac: 0.05     # up to ~50% by the last step
  batch_size: 8
  max_length: 512
  reference_model_path: "GSAI-ML/LLaDA-8B-Base"
  reference_device: "cuda"
  seed: 42
  save_metadata: false
  metadata_dir: ""  # Set via SAMA_METADATA_DIR env variable, or specify a path here

Mtc5depend-4-0.1-layer-0-1-least1-mask-0.05-0.20:
  module: "mtc5depend"
  lambda_penalty: 0.1
  leastm: 1
  weight_start_layer_ratio: 0
  weight_end_layer_ratio: 1
  steps: 4
  subset_size: 8          # l for each local subset vote
  num_subsets: 128        # number of subsets per step
  l_schedule: "linear"    # or "geometric"
  min_mask_frac: 0.05     # ~5% of valid tokens masked at early steps
  max_mask_frac: 0.20     # up to ~50% by the last step
  batch_size: 8
  max_length: 512
  reference_model_path: "GSAI-ML/LLaDA-8B-Base"
  reference_device: "cuda"
  seed: 42
  save_metadata: false
  metadata_dir: ""  # Set via SAMA_METADATA_DIR env variable, or specify a path here

Mtc5depend-4-0.1-layer-0-1-least1-mask-0.05-0.30:
  module: "mtc5depend"
  lambda_penalty: 0.1
  leastm: 1
  weight_start_layer_ratio: 0
  weight_end_layer_ratio: 1
  steps: 4
  subset_size: 8          # l for each local subset vote
  num_subsets: 128        # number of subsets per step
  l_schedule: "linear"    # or "geometric"
  min_mask_frac: 0.05     # ~5% of valid tokens masked at early steps
  max_mask_frac: 0.30     # up to ~50% by the last step
  batch_size: 8
  max_length: 512
  reference_model_path: "GSAI-ML/LLaDA-8B-Base"
  reference_device: "cuda"
  seed: 42
  save_metadata: false
  metadata_dir: ""  # Set via SAMA_METADATA_DIR env variable, or specify a path here

Mtc5depend-4-0.1-layer-0-1-least1-mask-0.05-0.40:
  module: "mtc5depend"
  lambda_penalty: 0.1
  leastm: 1
  weight_start_layer_ratio: 0
  weight_end_layer_ratio: 1
  steps: 4
  subset_size: 8          # l for each local subset vote
  num_subsets: 128        # number of subsets per step
  l_schedule: "linear"    # or "geometric"
  min_mask_frac: 0.05     # ~5% of valid tokens masked at early steps
  max_mask_frac: 0.40     # up to ~50% by the last step
  batch_size: 8
  max_length: 512
  reference_model_path: "GSAI-ML/LLaDA-8B-Base"
  reference_device: "cuda"
  seed: 42
  save_metadata: false
  metadata_dir: ""  # Set via SAMA_METADATA_DIR env variable, or specify a path here

